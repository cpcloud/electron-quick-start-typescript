{
  "name": "set",
  "version": "0.0.0",
  "description": "Set using Electron",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "lint": "tslint -c tslint.json -p tsconfig.json",
    "start": "npm run build && electron ./dist/main.js"
  },
  "repository": "https://github.com/cpcloud/set",
  "keywords": [
    "Electron",
    "typescript",
    "set",
    "game",
    "puzzle"
  ],
  "author": "Phillip Cloud",
  "license": "Apache-2.0",
  "devDependencies": {
    "electron": "^1.7.11",
    "tslint": "^5.9.1",
    "typescript": "^2.6.2"
  }
}
